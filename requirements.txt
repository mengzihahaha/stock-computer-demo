# ============================================================
# 项目依赖清单（requirements.txt）
# ------------------------------------------------------------
# 作用：列出项目需要的第三方库。
# 安装命令：pip install -r requirements.txt
# 每一行是「库名==版本号」，锁定版本能保证别人和你跑出一样的结果。
# ============================================================

streamlit==1.40.1      # 把 Python 脚本变成网页应用的框架（我们的 UI）
pandas==2.2.3          # 表格数据处理核心库（因子计算全靠它）
numpy==2.1.3           # 数值计算库，pandas 底层依赖它
yfinance==0.2.50       # 获取美股/全球市场行情数据
akshare==1.16.98       # 获取 A 股行情数据（免费，无需 token）
