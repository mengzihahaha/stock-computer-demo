# ============================================================
# 项目依赖清单（requirements.txt）
# ------------------------------------------------------------
# 作用：列出项目需要的第三方库。
# 安装命令：pip install -r requirements.txt
# 用 ">=" 而非 "=="，允许安装该版本以上的最新兼容版，
# 这样在较新的 Python(如云端 3.14) 上也能找到可用的预编译包。
# ============================================================

streamlit>=1.40        # 把 Python 脚本变成网页应用的框架（我们的 UI）
pandas>=2.2            # 表格数据处理核心库（因子计算全靠它）
numpy>=2.1             # 数值计算库，pandas 底层依赖它
yfinance>=0.2.50       # 获取美股/全球市场行情数据
akshare>=1.16          # 获取 A 股行情数据（免费，无需 token）
